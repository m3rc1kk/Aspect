server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;
    # Без $uri/ — иначе nginx редиректит /admin → /admin/ и ломается SPA
    location / {
        try_files $uri /index.html;
    }
}
